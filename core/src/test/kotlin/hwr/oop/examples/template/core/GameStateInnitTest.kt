package hwr.oop.examples.template.core

import org.assertj.core.api.Assertions.assertThat
import org.junit.jupiter.api.Test

class GameStateInnitTest {


//	@Test
//	fun `create game with id and player`() {
//		// given
//		val playerAlpha = Player("alpha", Deck(listOf(Card(CardType.TRESSURE, "test"))))
//		val playerBeta = Player("beta", Deck(listOf(Card(CardType.TRESSURE, "test"))))
//		val playerGamma = Player("gamma", Deck(listOf(Card(CardType.TRESSURE, "test"))))
//		val playerDelta = Player("delta", Deck(listOf(Card(CardType.TRESSURE, "test"))))
//
//		val game = GameState(1, listOf(playerAlpha, playerBeta))
//		// when
//		val gameID = game.Id()
//		val players: List<Player> = game.players()
//		// then
//		assertThat(gameID).isEqualTo(1)
//		assertThat(players).isEqualTo(listOf(playerAlpha, playerBeta))
//
//	}

}
